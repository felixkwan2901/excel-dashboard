name = "cde-data-upload"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Holds the handful of things that genuinely have no home in the tracked
# Excel workbook — the Weekly Job Check Sheet, the Job Completion
# Checklist, and each new job's "first entered the site" timestamp. Used
# to be browser localStorage only (worked, but didn't sync across
# devices); this is the same data, just reachable from anywhere now.
[[kv_namespaces]]
binding = "APP_DATA"
id = "1bed6e14dbf047ac8616ae21ed09a9f6"
